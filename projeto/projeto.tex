\documentclass[12pt]{article}

\usepackage[T1]{fontenc} %pacote que permite a hifenização de palavras acentuadas
\usepackage[utf8]{inputenc}
\usepackage[brazil]{babel}
\usepackage{graphicx} %pacote para inserir figuras
\usepackage{geometry} %pacote que pertmite dimensionar a página
\usepackage{setspace} %pacote que pertmite alterar epaçamento entre linhas
\usepackage{indentfirst} %indentação de parágrafos
\usepackage{times} %fonte Times New Roman
\usepackage{sectsty} %pacote que pertmite alterar o estilo de seções e sub-seções
\usepackage{listings} %pacote para inserir código fonte
\usepackage{nomencl} %pacote para siglas e abreviações
\usepackage{afterpage}

\usepackage[hyphens]{url}	% Permite a quebra de linha nos hyfens das urls
\usepackage[pdftex,         % Este pacote cria hiperlinks
bookmarks=true,
bookmarksnumbered=true]{hyperref}
\hypersetup{
	colorlinks,						%Configura como o link é apresentado
	citecolor=black,				%Configura a cor das citações
	filecolor=black,				%Configura a cor dos arquivos
	linkcolor=black,				%Configura a cor de links internos
	urlcolor=black,					%Configura a cor de links externos
	pdfauthor = {Gabriel Barco e Saulo da Mata},	%Information about the document
	pdftitle = {Gabriel Barco - Projeto de TCC}
}

% Este pacote deve vir depois do pacote hyperref
\usepackage[alf]{abntex2cite} %pacote de estilos bibliográficos da abnt, [alf] ou [num]

%alterando as margens para os padrões da abnt
%esquerda 3cm, superior 3cm, direita 2cm, inferior 2cm
\geometry{a4paper,left=3cm,top=3cm,right=2cm,bottom=2cm}

%definindo o espaçamento 1.5 como padrão do documento
\setstretch{1.5}

%alterando o estilo da fonte das seções e sub-seções
\sectionfont{\fontsize{12}{15}\selectfont}
\subsectionfont{\fontsize{12}{15}\selectfont}
\subsubsectionfont{\fontsize{12}{15}\selectfont}

%alterando a legenda das listagens
\addto\captionsbrazil{
\renewcommand{\lstlistingname}{Listagem}
}

%configurações para inserir códigos fonte
\lstset{
  numbers=left, %números das linhas a esquerda
  numberstyle=\tiny, %alerando o tamanho dos números das linhas
  basicstyle=\footnotesize, %alterando o tamanho da fonte
  stringstyle=\ttfamily, %alterando o estilo das strings que existem no código fonte
  showstringspaces=false, % não sublinhar espaços em branco
  captionpos=b, %posição da legenda
  frame=tb %adicionar bordas
}
%\renewcommand{\lstlistlistingname}{Lista de Listagens}

\makenomenclature
\renewcommand{\nomname}{Lista de Abreviações e Siglas}
% para a lista ser criada, é preciso executar, no terminal,
% pelo menos uma vez, o comando abaixo:
% makeindex projeto.nlo -s nomencl.ist -o projeto.nls

% manualmente corrigindo a hifenização de certas palavras
\hyphenation{I-no-va-ções Im-ple-men-ta-ção}

\begin{document}
\input pre-textuais/capa
\input pre-textuais/folha-rosto

\newpage
\listoffigures
\thispagestyle{empty}

%\newpage
%\listoftables
%\thispagestyle{empty}

%\newpage
%\lstlistoflistings
%\thispagestyle{empty}

% \newpage
% \printnomenclature[30mm]
% \thispagestyle{empty}

\newpage
\tableofcontents
\thispagestyle{empty}

\input textuais/introducao
\input textuais/referencial-teorico
\input textuais/metodologia
\input textuais/cronograma

\newpage
\addcontentsline{toc}{section}{REFERÊNCIAS}
\renewcommand{\refname}{REFERÊNCIAS}

\bibliography{pos-textuais/projeto}

\end{document}